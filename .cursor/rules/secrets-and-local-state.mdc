---
description: Secrets / 本地狀態檔保護規範（避免誤提交）
globs:
  - data/*
  - logs/*
  - src/**
alwaysApply: false
---

# Secrets / 本地狀態檔保護

## 禁止入庫（git）

- **禁止**將密碼、JWT secret、token、cookie、API keys 寫入 repo。
- `data/.admin_password`、`data/.jwt_secret` 為本地運行用秘密檔，只能存在本地（已由 `.gitignore` 忽略）。
- `data/*.db*`（SQLite DB/WAL/SHM）與 `logs/*.log` 只能作為本地狀態，不納入版本控管。

## 設計原則

- 新增設定一律放 `.env.example`（脫敏、給預設值或說明），真實值用 `.env`（已忽略）。
- 程式輸出配置摘要時必須脫敏（避免把 secrets 印到 log）。

